import { SearchRoomsPipe } from './search-rooms.pipe';

describe('SearchRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
